class CompareToken(object):

    def __init__(self, asks, bids, where, to, symbols, percent, currency):
        """Constructor"""
        self.asks = asks
        self.bids = bids
        self.where = where
        self.to = to
        self.symbols = symbols
        self.percent = percent
        self.currency = currency
